import { IconImageLayer as L7IconImageLayer } from '@antv/l7-composite-layers';
import { forwardRef, memo, useImperativeHandle } from 'react';
import { useCreateLayer, useLayerEvent } from "../../hooks";
export var IconImageLayer = /*#__PURE__*/memo( /*#__PURE__*/forwardRef(function IconImageLayer(props, ref) {
  var layerRef = useCreateLayer(L7IconImageLayer, props);
  useLayerEvent(layerRef.current, props);
  useImperativeHandle(ref, function () {
    return layerRef.current;
  });
  return null;
}));