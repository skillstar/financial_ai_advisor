import { ChoroplethLayer as L7ChoroplethLayer } from '@antv/l7-composite-layers';
import { forwardRef, memo, useImperativeHandle } from 'react';
import { LayerEventMap } from "../../hooks/use-layer-event/constant";
import { useCreateLayer, useLayerEvent } from "../../hooks";
var ChoroplethLayerEventMap = Object.assign(LayerEventMap, {
  onSelect: 'select',
  onUnselect: 'unselect'
});
export var ChoroplethLayer = /*#__PURE__*/memo( /*#__PURE__*/forwardRef(function ChoroplethLayer(props, ref) {
  var layerRef = useCreateLayer(L7ChoroplethLayer, props);
  useLayerEvent(layerRef.current, props, ChoroplethLayerEventMap);
  useImperativeHandle(ref, function () {
    return layerRef.current;
  });
  return null;
}));