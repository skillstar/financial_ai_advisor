"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.CustomControl = void 0;
var _react = require("react");
var _reactDom = require("react-dom");
var _hooks = require("../../LarkMap/hooks");
var _utils = require("../../../utils");
const CustomControl = props => {
  const {
    className,
    style,
    children,
    position = 'topleft',
    name
  } = props;
  const containerRef = (0, _react.useRef)(document.createElement('div'));
  const styleText = (0, _utils.getStyleText)(style);
  const onCreate = (0, _react.useCallback)(() => {
    return containerRef.current;
  }, []);
  const controlOptions = (0, _react.useMemo)(() => {
    return {
      position,
      name,
      className,
      style: styleText
    };
  }, [position, name, className, styleText]);
  (0, _hooks.useControl)(onCreate, undefined, controlOptions);

  // @ts-ignore
  return /*#__PURE__*/(0, _reactDom.createPortal)(children, containerRef.current);
};
exports.CustomControl = CustomControl;