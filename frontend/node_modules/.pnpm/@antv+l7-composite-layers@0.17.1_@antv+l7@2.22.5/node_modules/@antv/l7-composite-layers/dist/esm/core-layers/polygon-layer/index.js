import { PolygonLayer as L7PolygonLayer } from '@antv/l7';
import { CoreLayer } from '../../core/core-layer';
/**
 * 面图层
 * 对应 L7 的 PolygonLayer
 **/
export class PolygonLayer extends CoreLayer {
    constructor() {
        super(...arguments);
        this.type = 'polygonLayer';
    }
    /**
     * 创建图层
     */
    createLayer() {
        const config = this.getLayerConfig();
        const layer = new L7PolygonLayer(config);
        return layer;
    }
}
//# sourceMappingURL=index.js.map