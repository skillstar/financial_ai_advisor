import { BubbleLayer as L7BubbleLayer } from '@antv/l7-composite-layers';
import { forwardRef, memo, useImperativeHandle } from 'react';
import { LayerEventMap } from "../../hooks/use-layer-event/constant";
import { useCreateLayer, useLayerEvent } from "../../hooks";
var BubbleLayerEventMap = Object.assign(LayerEventMap, {
  onSelect: 'select',
  onUnselect: 'unselect'
});
export var BubbleLayer = /*#__PURE__*/memo( /*#__PURE__*/forwardRef(function BubbleLayer(props, ref) {
  var layerRef = useCreateLayer(L7BubbleLayer, props);
  useLayerEvent(layerRef.current, props, BubbleLayerEventMap);
  useImperativeHandle(ref, function () {
    return layerRef.current;
  });
  return null;
}));