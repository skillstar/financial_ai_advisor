"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getStyleText = void 0;
var _lodash = require("lodash");
/**
 * 将 React.CSSProperties 格式的样式对象 转成 字符串格式
 * @param style
 */
const getStyleText = (style = {}) => {
  return Object.entries(style ?? {}).map(([key, value]) => {
    const cssKey = (0, _lodash.kebabCase)(key);
    let cssValue = value;
    if (typeof cssValue === 'number') {
      cssValue = `${cssValue}px`;
    } else if (typeof cssValue === 'string') {
      cssValue = cssValue.replace("'", '');
    }
    return `${cssKey} :${cssValue}`;
  }).join('; ');
};
exports.getStyleText = getStyleText;