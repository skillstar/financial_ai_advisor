import { CoreLayer } from '../../core/core-layer';
import { ILayer } from '../../types';
import { RasterLayerOptions } from './types';
/**
 * 栅格图层
 * 对应 L7 的 RasterLayer
 **/
export declare class RasterLayer extends CoreLayer<RasterLayerOptions> {
    type: string;
    /**
     * 创建图层
     */
    protected createLayer(): ILayer;
}
