import { LineLayer as L7LineLayer } from '@antv/l7';
import { CoreLayer } from '../../core/core-layer';
/**
 * 线图层
 * 对应 L7 的 LineLayer
 **/
export class LineLayer extends CoreLayer {
    constructor() {
        super(...arguments);
        this.type = 'lineLayer';
    }
    /**
     * 创建图层
     */
    createLayer() {
        const config = this.getLayerConfig();
        const layer = new L7LineLayer(config);
        return layer;
    }
}
//# sourceMappingURL=index.js.map