import { BubbleLayerActiveOptions, BubbleLayerOptions } from './types';
/**
 * 默认的全部交互状态配置
 */
export declare const DEFAULT_STATE: {
    active: Required<BubbleLayerActiveOptions>;
    select: Required<BubbleLayerActiveOptions>;
};
/**
 * 默认配置项
 */
export declare const DEFAULT_OPTIONS: Partial<BubbleLayerOptions>;
