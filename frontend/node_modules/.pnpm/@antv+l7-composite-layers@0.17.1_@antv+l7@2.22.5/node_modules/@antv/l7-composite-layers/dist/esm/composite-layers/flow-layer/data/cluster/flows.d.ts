import { ClusterFlow, OriginFlow } from '../../types';
import { buildIndex } from '../build-index';
/**
 * 计算客流聚合线
 * @param originFlows
 * @param clusterIndex
 * @param zoom
 * @returns
 */
export declare function clusterFlows(originFlows: OriginFlow[], clusterIndex: ReturnType<typeof buildIndex>, zoom: number): ClusterFlow[];
