import { clusterByHCA } from './hca';
export function clusterLocations(locations, state) {
    if (state.clusterType === 'HCA') {
        return clusterByHCA(locations, state);
    }
    return [];
}
export { clusterFlows } from './flows';
//# sourceMappingURL=index.js.map