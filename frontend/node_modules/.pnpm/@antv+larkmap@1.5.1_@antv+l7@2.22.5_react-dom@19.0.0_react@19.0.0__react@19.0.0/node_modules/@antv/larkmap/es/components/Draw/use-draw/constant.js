import { DrawCircle, DrawLine, DrawPoint, DrawPolygon, DrawRect } from '@antv/l7-draw';
export var DRAW_TYPE_MAP = {
  point: DrawPoint,
  line: DrawLine,
  polygon: DrawPolygon,
  rect: DrawRect,
  circle: DrawCircle
};