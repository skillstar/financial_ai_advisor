import { ImageLayer as L7ImageLayer } from '@antv/l7';
import { CoreLayer } from '../../core/core-layer';
/**
 * 图片图层
 * 对应 L7 的 ImageLayer
 **/
export class ImageLayer extends CoreLayer {
    constructor() {
        super(...arguments);
        this.type = 'imageLayer';
    }
    /**
     * 创建图层
     */
    createLayer() {
        const config = this.getLayerConfig();
        const layer = new L7ImageLayer(config);
        return layer;
    }
}
//# sourceMappingURL=index.js.map