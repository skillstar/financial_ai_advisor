import { ClusterLevel, ClusterState, OriginLocation } from '../../types';
/**
 * 根据 HCA 算法生成各个层级下的聚合点
 * @param locations
 * @param state
 * @returns
 */
export declare function clusterByHCA(locations: OriginLocation[], state: ClusterState): ClusterLevel[];
