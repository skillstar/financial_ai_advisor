import { useContext } from 'react';
import { LarkMapContext } from "../../index";
export var useLayerManager = function useLayerManager() {
  var context = useContext(LarkMapContext);
  if (!context) {
    throw new Error('The useLayerManager must be used in the LarkMap container');
  }
  var layerManager = context.layerManager;
  return layerManager;
};