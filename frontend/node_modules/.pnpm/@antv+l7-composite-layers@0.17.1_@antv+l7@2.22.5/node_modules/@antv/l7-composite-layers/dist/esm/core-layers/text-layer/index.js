import { PointLayer as L7PointLayer } from '@antv/l7';
import { CoreLayer } from '../../core/core-layer';
/**
 * 文本图层
 * 对应 L7 的 PointLayer
 **/
export class TextLayer extends CoreLayer {
    constructor() {
        super(...arguments);
        this.type = 'textLayer';
    }
    /**
     * 创建图层
     */
    createLayer() {
        const config = this.getLayerConfig();
        const layer = new L7PointLayer(config);
        return layer;
    }
    /**
     * 适配属性配置
     */
    adaptorAttrOptions(options) {
        const { field, style = {} } = options;
        const { fill: color, fontSize: size } = style || {};
        const shape = { field, value: 'text' };
        return Object.assign({ shape, color, size }, options);
    }
}
//# sourceMappingURL=index.js.map