"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getGradientColors = void 0;
var _color = _interopRequireDefault(require("color"));
var _lodash = require("lodash");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
const getGradientColors = (colorStart, colorEnd, count = 5) => {
  const singleColors = [colorStart]; // 初始颜色

  const colorMinInt = (0, _color.default)(colorStart).object();
  const colorMaxInt = (0, _color.default)(colorEnd).object();
  if (count < 2) {
    return count < 1 ? [] : singleColors;
  }
  const diffNumber = {
    r: (colorMinInt.r - colorMaxInt.r) / (count - 1),
    g: (colorMinInt.g - colorMaxInt.g) / (count - 1),
    b: (colorMinInt.b - colorMaxInt.b) / (count - 1)
  };
  (0, _lodash.forEach)(new Array(count - 2), (item, index) => singleColors.push((0, _color.default)({
    r: (0, _lodash.round)(colorMinInt.r - diffNumber.r * (index + 1), 0),
    g: (0, _lodash.round)(colorMinInt.g - diffNumber.g * (index + 1), 0),
    b: (0, _lodash.round)(colorMinInt.b - diffNumber.b * (index + 1), 0)
  }).hex()));
  singleColors.push(colorEnd);
  return singleColors;
};
exports.getGradientColors = getGradientColors;