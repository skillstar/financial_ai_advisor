import { ClusterState, OriginLocation } from '../../types';
export declare function clusterLocations(locations: OriginLocation[], state: ClusterState): import("../../types").ClusterLevel[];
export { clusterFlows } from './flows';
