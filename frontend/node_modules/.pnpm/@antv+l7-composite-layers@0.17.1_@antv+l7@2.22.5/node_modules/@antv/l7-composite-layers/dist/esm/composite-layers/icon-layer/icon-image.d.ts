import { Scene } from '../../types';
import { IconLayer } from './icon';
import { IconImageLayerOptions } from './types';
export declare class IconImageLayer extends IconLayer<IconImageLayerOptions> {
    /**
     * 默认配置项
     */
    static DefaultOptions: Partial<import("./types").IconLayerOptions>;
    /**
     * 复合图层类型
     */
    type: import("../../types").CompositeLayerType;
    /**
     * 添加到场景
     */
    addTo(scene: Scene): void;
    /**
     * 初始化资源
     */
    protected initAssets(): Promise<void>;
    /**
     * 更新资源
     */
    private updateAssets;
    protected updateSubLayers(options: Partial<IconImageLayerOptions>): void;
    /**
     * load 图片资源
     */
    protected loadIconAtlas(): Promise<void>;
}
