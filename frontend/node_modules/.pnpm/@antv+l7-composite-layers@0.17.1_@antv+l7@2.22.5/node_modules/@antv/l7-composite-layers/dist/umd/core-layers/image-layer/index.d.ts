import { CoreLayer } from '../../core/core-layer';
import { ILayer } from '../../types';
import { ImageLayerOptions } from './types';
/**
 * 图片图层
 * 对应 L7 的 ImageLayer
 **/
export declare class ImageLayer extends CoreLayer<ImageLayerOptions> {
    type: string;
    /**
     * 创建图层
     */
    protected createLayer(): ILayer;
}
