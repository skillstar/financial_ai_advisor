import { RasterLayer as L7RasterLayer } from '@antv/l7';
import { CoreLayer } from '../../core/core-layer';
/**
 * 栅格图层
 * 对应 L7 的 RasterLayer
 **/
export class RasterLayer extends CoreLayer {
    constructor() {
        super(...arguments);
        this.type = 'rasterLayer';
    }
    /**
     * 创建图层
     */
    createLayer() {
        const config = this.getLayerConfig();
        const layer = new L7RasterLayer(config);
        return layer;
    }
}
//# sourceMappingURL=index.js.map