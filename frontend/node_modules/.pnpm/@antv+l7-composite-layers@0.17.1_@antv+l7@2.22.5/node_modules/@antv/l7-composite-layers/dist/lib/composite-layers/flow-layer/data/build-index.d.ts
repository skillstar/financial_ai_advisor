import { ClusterLevel, ClusterLocation } from '../types';
/**
 * 建立聚合点的检索索引对象
 * @param clusterLevels
 * @returns
 */
export declare function buildIndex(clusterLevels: ClusterLevel[]): {
    zoomList: number[];
    clusterIdMap: Map<string, ClusterLocation>;
    getAppropriateLevel: (targetZoom: number) => ClusterLevel;
    getMapLocations: (targetZoom: number, bounds: [number, number, number, number]) => ClusterLocation[];
    getLocationIdsFromCluster: (clusterId: string) => string[];
    findClusterForZoom: (locationId: string, zoom: number) => ClusterLocation | undefined;
};
