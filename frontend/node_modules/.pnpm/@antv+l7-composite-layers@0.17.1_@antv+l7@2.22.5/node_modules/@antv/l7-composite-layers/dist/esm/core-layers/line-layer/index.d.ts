import { CoreLayer } from '../../core/core-layer';
import { ILayer } from '../../types';
import { LineLayerOptions } from './types';
/**
 * 线图层
 * 对应 L7 的 LineLayer
 **/
export declare class LineLayer extends CoreLayer<LineLayerOptions> {
    type: string;
    /**
     * 创建图层
     */
    protected createLayer(): ILayer;
}
