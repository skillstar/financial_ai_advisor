import { CoreLayer } from '../../core/core-layer';
import { ILayer } from '../../types';
import { PointLayerOptions } from './types';
/**
 * 点图层
 * 对应 L7 的 PointLayer
 **/
export declare class PointLayer extends CoreLayer<PointLayerOptions> {
    type: string;
    /**
     * 创建图层
     */
    protected createLayer(): ILayer;
}
