import type { DualAxesSeriesItem } from '.';
export declare function transform(series: DualAxesSeriesItem[], categories: string[]): {
    children: any[];
    data: {
        [key: string]: any;
        category: string;
    }[];
    legendTypeList: ("rect" | "smooth")[];
};
